//create function using function keyword that take string as an argument & return the number of vowels in the string:

function countVowels(str){
    let count=0;
    for(const char of str){
        if(char==="a" || char==="e" || char==="i" || char=="o" || char==="u"){
            count ++;
        }
    }
   console.log(count); 
}   
